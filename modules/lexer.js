const lexthisshit = (input) => {
    let tokens = [];
    let current = 0;

    while(input.length > current) {
        if(input[current] === 'A'){
            tokens.push({
                type: 'caps_str',
                value: 'A'
            });
            current++;
            continue;
        }

        if(input[current] === 'B'){
            tokens.push({
                type: 'caps_str',
                value: 'B'
            });
            current++;
            continue;
        }

        if(input[current] === 'C'){
            tokens.push({
                type: 'caps_str',
                value: 'C'
            });
            current++;
            continue;
        }

        if(input[current] === 'D'){
            tokens.push({
                type: 'caps_str',
                value: 'D'
            });
            current++;
            continue;
        }

        if(input[current] === 'E'){
            tokens.push({
                type: 'caps_str',
                value: 'E'
            });
            current++;
            continue;
        }

        if(input[current] === 'F'){
            tokens.push({
                type: 'caps_str',
                value: 'F'
            });
            current++;
            continue;
        }

        if(input[current] === 'G'){
            tokens.push({
                type: 'caps_str',
                value: 'G'
            });
            current++;
            continue;
        }

        if(input[current] === 'H'){
            tokens.push({
                type: 'caps_str',
                value: 'H'
            });
            current++;
            continue;
        }

        if(input[current] === 'I'){
            tokens.push({
                type: 'caps_str',
                value: 'I'
            });
            current++;
            continue;
        }

        if(input[current] === 'J'){
            tokens.push({
                type: 'caps_str',
                value: 'J'
            });
            current++;
            continue;
        }

        if(input[current] === 'K'){
            tokens.push({
                type: 'caps_str',
                value: 'K'
            });
            current++;
            continue;
        }

        if(input[current] === 'L'){
            tokens.push({
                type: 'caps_str',
                value: 'L'
            });
            current++;
            continue;
        }

        if(input[current] === 'M'){
            tokens.push({
                type: 'caps_str',
                value: 'M'
            });
            current++;
            continue;
        }

        if(input[current] === 'N'){
            tokens.push({
                type: 'caps_str',
                value: 'N'
            });
            current++;
            continue;
        }

        if(input[current] === 'O'){
            tokens.push({
                type: 'caps_str',
                value: 'O'
            });
            current++;
            continue;
        }

        if(input[current] === 'P'){
            tokens.push({
                type: 'caps_str',
                value: 'P'
            });
            current++;
            continue;
        }

        if(input[current] === 'Q'){
            tokens.push({
                type: 'caps_str',
                value: 'Q'
            });
            current++;
            continue;
        }

        if(input[current] === 'R'){
            tokens.push({
                type: 'caps_str',
                value: 'R'
            });
            current++;
            continue;
        }

        if(input[current] === 'S'){
            tokens.push({
                type: 'caps_str',
                value: 'S'
            });
            current++;
            continue;
        }

        if(input[current] === 'T'){
            tokens.push({
                type: 'caps_str',
                value: 'T'
            });
            current++;
            continue;
        }

        if(input[current] === 'U'){
            tokens.push({
                type: 'caps_str',
                value: 'U'
            });
            current++;
            continue;
        }

        if(input[current] === 'V'){
            tokens.push({
                type: 'caps_str',
                value: 'V'
            });
            current++;
            continue;
        }

        if(input[current] === 'W'){
            tokens.push({
                type: 'caps_str',
                value: 'W'
            });
            current++;
            continue;
        }

        if(input[current] === 'X'){
            tokens.push({
                type: 'caps_str',
                value: 'X'
            });
            current++;
            continue;
        }

        if(input[current] === 'Y'){
            tokens.push({
                type: 'caps_str',
                value: 'Y'
            });
            current++;
            continue;
        }

        if(input[current] === 'Z'){
            tokens.push({
                type: 'caps_str',
                value: 'Z'
            });
            current++;
            continue;
        }

        if(input[current] === 'a'){
            tokens.push({
                type: 'small_str',
                value: 'a'
            });
            current++;
            continue;
        }

        if(input[current] === 'b'){
            tokens.push({
                type: 'small_str',
                value: 'b'
            });
            current++;
            continue;
        }

        if(input[current] === 'c'){
            tokens.push({
                type: 'small_str',
                value: 'c'
            });
            current++;
            continue;
        }

        if(input[current] === 'd'){
            tokens.push({
                type: 'small_str',
                value: 'd'
            });
            current++;
            continue;
        }

        if(input[current] === 'e'){
            tokens.push({
                type: 'small_str',
                value: 'e'
            });
            current++;
            continue;
        }

        if(input[current] === 'f'){
            tokens.push({
                type: 'small_str',
                value: 'f'
            });
            current++;
            continue;
        }

        if(input[current] === 'g'){
            tokens.push({
                type: 'small_str',
                value: 'g'
            });
            current++;
            continue;
        }

        if(input[current] === 'h'){
            tokens.push({
                type: 'small_str',
                value: 'h'
            });
            current++;
            continue;
        }

        if(input[current] === 'i'){
            tokens.push({
                type: 'small_str',
                value: 'i'
            });
            current++;
            continue;
        }

        if(input[current] === 'j'){
            tokens.push({
                type: 'small_str',
                value: 'j'
            });
            current++;
            continue;
        }

        if(input[current] === 'k'){
            tokens.push({
                type: 'small_str',
                value: 'k'
            });
            current++;
            continue;
        }

        if(input[current] === 'l'){
            tokens.push({
                type: 'small_str',
                value: 'l'
            });
            current++;
            continue;
        }

        if(input[current] === 'm'){
            tokens.push({
                type: 'small_str',
                value: 'm'
            });
            current++;
            continue;
        }

        if(input[current] === 'n'){
            tokens.push({
                type: 'small_str',
                value: 'n'
            });
            current++;
            continue;
        }

        if(input[current] === 'o'){
            tokens.push({
                type: 'small_str',
                value: 'o'
            });
            current++;
            continue;
        }

        if(input[current] === 'p'){
            tokens.push({
                type: 'small_str',
                value: 'p'
            });
            current++;
            continue;
        }

        if(input[current] === 'q'){
            tokens.push({
                type: 'small_str',
                value: 'q'
            });
            current++;
            continue;
        }

        if(input[current] === 'r'){
            tokens.push({
                type: 'small_str',
                value: 'r'
            });
            current++;
            continue;
        }

        if(input[current] === 's'){
            tokens.push({
                type: 'small_str',
                value: 's'
            });
            current++;
            continue;
        }

        if(input[current] === 't'){
            tokens.push({
                type: 'small_str',
                value: 't'
            });
            current++;
            continue;
        }

        if(input[current] === 'u'){
            tokens.push({
                type: 'small_str',
                value: 'u'
            });
            current++;
            continue;
        }

        if(input[current] === 'v'){
            tokens.push({
                type: 'small_str',
                value: 'v'
            });
            current++;
            continue;
        }

        if(input[current] === 'w'){
            tokens.push({
                type: 'small_str',
                value: 'w'
            });
            current++;
            continue;
        }

        if(input[current] === 'x'){
            tokens.push({
                type: 'small_str',
                value: 'x'
            });
            current++;
            continue;
        }

        if(input[current] === 'y'){
            tokens.push({
                type: 'small_str',
                value: 'y'
            });
            current++;
            continue;
        }

        if(input[current] === 'z'){
            tokens.push({
                type: 'small_str',
                value: 'z'
            });
            current++;
            continue;
        }

        if(input[current] === ' '){
            tokens.push({
                type: 'space',
                value: ' '
            });
            current++;
            continue;
        }

        if(input[current] === '.'){
            tokens.push({
                type: 'dot',
                value: '.'
            });
            current++;
            continue;
        }

        if(input[current] === ','){
            tokens.push({
                type: 'comma',
                value: ','
            });
            current++;
            continue;
        }

        if(input[current] === ';'){
            tokens.push({
                type: 'semicolon',
                value: ';'
            });
            current++;
            continue;
        }

        if(input[current] === ':'){
            tokens.push({
                type: 'colon',
                value: ':'
            });
            current++;
            continue;
        }

        if(input[current] === '?'){
            tokens.push({
                type: 'question_mark',
                value: '?'
            });
            current++;
            continue;
        }

        if(input[current] === '!'){
            tokens.push({
                type: 'exclamation_mark',
                value: '!'
            });
            current++;
            continue;
        }

        if(input[current] === '@'){
            tokens.push({
                type: 'at',
                value: '@'
            });
            current++;
            continue;
        }

        if(input[current] === '#'){
            tokens.push({
                type: 'hash',
                value: '#'
            });
            current++;
            continue;
        }

        if(input[current] === '$'){
            tokens.push({
                type: 'dollar',
                value: '$'
            });
            current++;
            continue;
        }

        if(input[current] === '%'){
            tokens.push({
                type: 'percentage',
                value: '%'
            });
            current++;
            continue;
        }

        if(input[current] === '^'){
            tokens.push({
                type: 'caret',
                value: '^'
            });
            current++;
            continue;
        }

        if(input[current] === '&'){
            tokens.push({
                type: 'ampersand',
                value: '&'
            });
            current++;
            continue;
        }

        if(input[current] === '*'){
            tokens.push({
                type: 'asterisk',
                value: '*'
            });
            current++;
            continue;
        }

        if(input[current] === '('){
            tokens.push({
                type: 'opening_parenthesis',
                value: '('
            });
            current++;
            continue;
        }

        if(input[current] === ')'){
            tokens.push({
                type: 'closing_parenthesis',
                value: ')'
            });
            current++;
            continue;
        }

        if(input[current] === '-'){
            tokens.push({
                type: 'hyphen',
                value: '-'
            });
            current++;
            continue;
        }

        if(input[current] === '_'){
            tokens.push({
                type: 'underscore',
                value: '_'
            });
            current++;
            continue;
        }

        if(input[current] === '='){
            tokens.push({
                type: 'equal',
                value: '='
            });
            current++;
            continue;
        }

        if(input[current] === '+'){
            tokens.push({
                type: 'plus',
                value: '+'
            });
            current++;
            continue;
        }

        if(input[current] === '['){
            tokens.push({
                type: 'opening_square_bracket',
                value: '['
            });
            current++;
            continue;
        }

        if(input[current] === ']'){
            tokens.push({
                type: 'closing_square_bracket',
                value: ']'
            });
            current++;
            continue;
        }

        if(input[current] === '{'){
            tokens.push({
                type: 'opening_curly_bracket',
                value: '{'
            });
            current++;
            continue;
        }

        if(input[current] === '}'){
            tokens.push({
                type: 'closing_curly_bracket',
                value: '}'
            });
            current++;
            continue;
        }

        if(input[current] === '|'){
            tokens.push({
                type: 'pipe',
                value: '|'
            });
            current++;
            continue;
        }

        if(input[current] === '0'){
            tokens.push({
                type: 'number',
                value: '0'
            });
            current++;
            continue;
        }

        if(input[current] === '1'){
            tokens.push({
                type: 'number',
                value: '1'
            });
            current++;
            continue;
        }

        if(input[current] === '2'){
            tokens.push({
                type: 'number',
                value: '2'
            });
            current++;
            continue;
        }

        if(input[current] === '3'){
            tokens.push({
                type: 'number',
                value: '3'
            });
            current++;
            continue;
        }

        if(input[current] === '4'){
            tokens.push({
                type: 'number',
                value: '4'
            });
            current++;
            continue;
        }

        if(input[current] === '5'){
            tokens.push({
                type: 'number',
                value: '5'
            });
            current++;
            continue;
        }

        if(input[current] === '6'){
            tokens.push({
                type: 'number',
                value: '6'
            });
            current++;
            continue;
        }

        if(input[current] === '7'){
            tokens.push({
                type: 'number',
                value: '7'
            });
            current++;
            continue;
        }

        if(input[current] === '8'){
            tokens.push({
                type: 'number',
                value: '8'
            });
            current++;
            continue;
        }

        if(input[current] === '9'){
            tokens.push({
                type: 'number',
                value: '9'
            });
            current++;
            continue;
        }

        if(input[current] === '"'){
            tokens.push({
                type: 'double_quote',
                value: '"'
            });
            current++;
            continue;
        }
    }

    return tokens;
}

module.exports = lexthisshit;